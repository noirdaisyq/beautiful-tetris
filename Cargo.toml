[package]
name = "beautiful-tetris"
version = "0.1.0"
edition = "2021"

[dependencies]
macroquad = { version = "0.4.14", features = ["audio"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
